apiVersion: v1
kind: Pod
metadata:
  name: smaple-pod
  namespace: roboshop # if we dont give namespace, default namespace will be assainged
spec:
  containers:
    - name: nginx-sample
      image: nginx:1.14:2
      ports:
      - containerPort: 80